[package]
name = "ac-formats"
description = "Decoders for the asset types stored in Asheron's Call DAT archives"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
glam = { version = "0.30", features = ["serde"] }
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
ac-dat = { path = "../ac-dat" }
