<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The hardened runtime is required for notarization; nothing here
         needs an exception (no JIT, no unsigned libraries), so the list
         stays empty on purpose. -->
</dict>
</plist>
